version: "3"

env:
  DATABASE_URL: postgres://postgres:password@postgres:5432/testdb?sslmode=disable

includes:
  mise: tasks/MiseTasks.yml
  yml: tasks/YamlTasks.yml
  json: tasks/JsonTasks.yml
  md: tasks/MarkdownTasks.yml
  gh: tasks/GitHubTasks.yml
  pg: tasks/PgTasks.yml
  dm: tasks/DbMigrationTasks.yml

tasks:
  default:
    cmds:
      - task: list
      - task: mise:versions

  list:
    desc: List all tasks
    cmd: task --list-all
    aliases:
      - l

  select-run:
    desc: "Select and run with fzf"
    cmds:
      - ./tasks/task-select-run.sh
    aliases:
      - sr

  setup:
    desc: "Setup project"
    cmds:
      - task: mise:setup
    aliases:
      - s

  check:
    desc: "Check"
    deps:
      - yml:check
      - json:check
      - md:check
      - gh:check
    aliases:
      - c
