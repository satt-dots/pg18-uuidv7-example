version: "3"

tasks:
  check:
    desc: "Check JSON"
    cmds:
      - task: fmt
      - task: lint

  fmt:
    desc: "Format JSON"
    cmds:
      - biome format --write .
    aliases:
      - fix

  lint:
    desc: "Lint JSON"
    cmds:
      - biome lint .
